# LEAP Batch Jobs [DRAFT]

Run batch jobs on GCP via [SkyPilot](https://skypilot.readthedocs.io) and GitHub Actions.

## Usage

1. Edit `batch/batch.py` with your batch script.
2. Add dependencies to `pyproject.toml` (`uv add <package>`)

